"use strict";

function HeadquarterEntity() {}

HeadquarterEntity.prototype.index = function (data) {
  // Implement the logic to build the response entity for headquarters index operation
  return data;
};

HeadquarterEntity.prototype.show = function (data) {
  // Implement the logic to build the response entity for headquarters show operation
  return data;
};

HeadquarterEntity.prototype.create = function (data) {
  // Implement the logic to build the response entity for headquarters create operation
  return data;
};

HeadquarterEntity.prototype.update = function (data) {
  // Implement the logic to build the response entity for headquarters update operation
  return data;
};

module.exports = HeadquarterEntity;
