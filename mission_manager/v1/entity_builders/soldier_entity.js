"use strict";

function SoldierEntity() {}

SoldierEntity.prototype.index = function (data) {
  // Implement the logic to build the response entity for soldiers index operation
  return data;
};

SoldierEntity.prototype.show = function (data) {
  // Implement the logic to build the response entity for soldiers show operation
  return data;
};

SoldierEntity.prototype.create = function (data) {
  // Implement the logic to build the response entity for soldiers create operation
  return data;
};

SoldierEntity.prototype.update = function (data) {
  // Implement the logic to build the response entity for soldiers update operation
  return data;
};

module.exports = SoldierEntity;
